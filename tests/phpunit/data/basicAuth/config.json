{
  "parameters": {
    "api": {
      "baseUrl": "http://jsontest",
      "authentication": {
        "type": "basic"
      }
    },
    "config": {
      "username": "test",
      "password": "test",
      "outputBucket": "getPost",
      "jobs": [
        {
          "endpoint": "basic",
          "dataType": "get"
        }
      ]
    }
  }
}
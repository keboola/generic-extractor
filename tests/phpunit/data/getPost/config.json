{
  "parameters": {
    "api": {
      "baseUrl": "http://jsontest",
      "pagination": {
        "method": "response.url",
        "urlKey": "next"
      }
    },
    "config": {
      "outputBucket": "getPost",
      "jobs": [
        {
          "endpoint": "get"
        },
        {
          "endpoint": "post",
          "method": "POST",
          "params": {
            "parameter": "value"
          }
        }
      ]
    }
  }
}
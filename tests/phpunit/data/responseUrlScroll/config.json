{
  "parameters": {
    "api": {
      "baseUrl": "http://jsontest",
      "pagination": {
        "method": "response.url",
        "urlKey": "next"
      }
    },
    "config": {
      "outputBucket": "responseUrl",
      "jobs": [
        {
          "endpoint": "scroll"
        }
      ]
    }
  }
}
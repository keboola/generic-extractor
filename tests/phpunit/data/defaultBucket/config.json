{
  "parameters": {
    "api": {
      "baseUrl": "http://jsontest"
    },
    "config": {
      "jobs": [
        {
          "endpoint": "defaultBucket",
          "dataType": "db"
        }
      ]
    }
  }
}
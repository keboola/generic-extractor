{
  "parameters": {
    "api": {
      "baseUrl": "http://jsontest",
      "http": {
        "defaultOptions": {
          "params": {
            "param": "value"
          }
        }
      }
    },
    "config": {
      "outputBucket": "defaultOptions",
      "debug": 1,
      "jobs": [
        {
          "endpoint": "defaultOptions"
        }
      ]
    }
  }
}
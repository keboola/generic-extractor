parameters:
    api:
        baseUrl: http://private-53977-extractormock.apiary-mock.com/
    config:
        jobs:
            -
                endpoint: get
    iterations:
        -
            outputBucket: first
            userData: a scalar
        -
            outputBucket: second
            userData:
                an: array

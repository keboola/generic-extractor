{
  "parameters": {
    "api": {
      "baseUrl": "http://private-74f7c-extractormock.apiary-mock.com",
      "pagination": {
        "method": "response.url",
        "urlKey": "next"
      }
    },
    "config": {
      "outputBucket": "responseUrl",
      "jobs": [
        {
          "endpoint": "scroll"
        }
      ]
    }
  }
}
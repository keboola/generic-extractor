parameters:
    api:
        baseUrl: http://private-53977-extractormock.apiary-mock.com/
    config:
        #debug: 1
        jobs:
            -
                endpoint: defaultBucket
                dataType: db

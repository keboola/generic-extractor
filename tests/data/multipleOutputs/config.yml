parameters:
    api:
        baseUrl: http://private-74f7c-extractormock.apiary-mock.com
    config:
        jobs:
            -
                endpoint: get
    iterations:
        -
            outputBucket: first
        -
            outputBucket: second
            incrementalOutput: true

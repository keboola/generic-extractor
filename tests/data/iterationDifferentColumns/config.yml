parameters:
    api:
        baseUrl: http://private-53977-extractormock.apiary-mock.com/
    config:
        outputBucket: test
        jobs:
            -
                endpoint: get
    iterations:
        -
            userData: a scalar
        -
            userData:
                an: array

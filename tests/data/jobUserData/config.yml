parameters:
    api:
        baseUrl: http://private-53977-extractormock.apiary-mock.com/
    config:
        userData:
            cfg: cfgVal
        jobs:
            -
                endpoint: get
                userData:
                    job: jobVal

parameters:
    api:
        baseUrl: http://private-53977-extractormock.apiary-mock.com/
        pagination:
            method: response.url
            urlKey: next
    config:
        outputBucket: getPost
        jobs:
            -
                endpoint: get
                #dataType: tags
            -
                endpoint: post
                method: POST
                params:
                    parameter: value

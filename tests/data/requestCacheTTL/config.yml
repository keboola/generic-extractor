parameters:
    cache:
      ttl: 8
    api:
        baseUrl: http://www.timeapi.org/
    config:
#        debug: true
        outputBucket: getPost
        jobs:
            -
                endpoint: utc/now.json
                dataType: get
                dataField: dateString
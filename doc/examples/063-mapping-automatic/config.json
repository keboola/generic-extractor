{
    "parameters": {
        "api": {
            "baseUrl": "http://mock-server:80/063-mapping-automatic/"
        },
        "config": {
            "debug": true,
            "outputBucket": "mock-server",
            "jobs": [
                {
                    "endpoint": "users"
                }
            ]
        }
    }
}

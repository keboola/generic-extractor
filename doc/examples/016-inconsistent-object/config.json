{
    "parameters": {
        "api": {
            "baseUrl": "http://mock-server:80/016-inconsistent-object/"
        },
        "config": {
            "debug": true,
            "outputBucket": "mock-server",
            "jobs": [
                {
                    "endpoint": "users",
                    "responseFilter": "color"
                }
            ]
        }
    }
}

version: '3'
services:
  app:
    build: .
    image: keboola/generic-extractor
    volumes:
      - ./:/code
      - ./data:/data

  dev:
    build:
      context: .
      dockerfile: Dockerfile-tests
    volumes:
      - ./:/code
      - ./data:/data

version: '2'

services:
  extractor:
    build: .
    image: keboola/generic-extractor
    volumes:
      - .:/home
    working_dir: /home
    command: "php ./run.php --data=./data"
    tty: true
    stdin_open: true
  tests:
    build: .
    volumes:
      - .:/home
    working_dir: /home
    command: "./phpunit.sh"
    tty: true
    stdin_open: true
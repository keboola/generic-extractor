version: '3'
services:
  app:
    build: .
    image: keboola/generic-extractor

  ci: &ci
    build:
      context: .
      dockerfile: Dockerfile-tests
    command: composer ci

  dev:
    << : *ci
    command: bash
    volumes:
      - ./:/code
      - ./data:/data
